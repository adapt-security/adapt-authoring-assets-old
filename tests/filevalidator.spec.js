const should = require('should');
const Filevalidator = require('../lib/filevalidator');

describe('Filevalidator module', function() { 
  it('Should return an object', function() {
    false.should.be.true();
  });
});