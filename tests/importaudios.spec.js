const should = require('should');
const Importaudios = require('../lib/importaudios');

describe('Importaudios module', function() { 
  it('Should return an array of audio objects', function() {
    false.should.be.true();
  });
});