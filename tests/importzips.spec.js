const should = require('should');
const Importzips = require('./../lib/importzips');

describe('Importzips module', function() { 
  it('Should return an array of objects', function() {
    false.should.be.true();
  });
});