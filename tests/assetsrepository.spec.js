const should = require('should');
const assetsrepository = require('../lib/assetsrepository');

describe('Assetsrepository module', function() { 
  it('Should return an object', function() {
    false.should.be.true();
  });
});