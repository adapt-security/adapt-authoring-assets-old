const should = require('should');
const Importimages = require('../lib/importimages');

describe('Importimages module', function() { 
  it('Should return an array of image objects', function() {
    false.should.be.true();
  });
});