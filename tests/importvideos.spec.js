const should = require('should');
const Importvideos = require('../lib/importvideos');

describe('Importvideos module', function() { 
  it('Should return an array of video objects', function() {
    false.should.be.true();
  });
});